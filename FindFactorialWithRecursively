def factorial(x):
    """This is a recursive fonction to find a factorial"""
    if x==1 or x==0:
        return 1
    else:
        return (x*factorial(x-1))

num=3
print("the factorial of", num, "is", factorial(num))
